function removeDuplicates(inputString) {
    // Split the input string into an array of strings
    var stringArray = inputString.split(',');

    // Use an object to keep track of unique strings
    var uniqueStringsObj = {};

    // Loop through the array and add each string to the object
    for (var i = 0; i < stringArray.length; i++) {
        uniqueStringsObj[stringArray[i]] = true;
    }

    // Extract unique strings from the object keys
    var uniqueStringsArray = Object.keys(uniqueStringsObj);

    // Join the array back into a string with commas
    var resultString = uniqueStringsArray.join(',');

    return resultString;
}

// Example usage:
var string_a = "apple,mango,apple,mango,carrot";
var string_b = removeDuplicates(string_a);
console.log(string_b);
// Output: apple,mango,carrot

